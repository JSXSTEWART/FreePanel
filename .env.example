# FreePanel Environment Configuration
# Copy this file to .env and configure for your server
#
# For AlmaLinux/Rocky Linux installation:
#   sudo bash system/scripts/install.sh
#

#==============================================================================
# APPLICATION SETTINGS
#==============================================================================

APP_NAME=FreePanel
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://your-server-ip:8080
APP_HOSTNAME=your-hostname.example.com

# Timezone (PHP format)
APP_TIMEZONE=UTC

#==============================================================================
# LOGGING
#==============================================================================

LOG_CHANNEL=daily
LOG_LEVEL=warning
LOG_DEPRECATIONS_CHANNEL=null

#==============================================================================
# DATABASE CONFIGURATION
#==============================================================================

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=freepanel
DB_USERNAME=freepanel
DB_PASSWORD=

#==============================================================================
# CACHE & SESSION (Redis recommended for production)
#==============================================================================

CACHE_DRIVER=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=1440
QUEUE_CONNECTION=redis

#==============================================================================
# REDIS CONFIGURATION
#==============================================================================

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_CLIENT=phpredis

#==============================================================================
# FREEPANEL SPECIFIC SETTINGS
#==============================================================================

# Server Identity
FREEPANEL_HOSTNAME=
FREEPANEL_SERVER_IP=

# Control Panel Ports
FREEPANEL_ADMIN_PORT=2087
FREEPANEL_USER_PORT=2083

# Service Selection
# Webserver: apache or nginx
FREEPANEL_WEBSERVER=apache

# DNS Server: bind or powerdns
FREEPANEL_DNS_SERVER=bind

# Mail Server: exim or postfix
FREEPANEL_MAIL_SERVER=exim

#==============================================================================
# SYSTEM PATHS (AlmaLinux/RHEL defaults)
#==============================================================================

# For AlmaLinux/Rocky Linux/RHEL:
FREEPANEL_VHOSTS_PATH=/etc/httpd/conf.d
FREEPANEL_VHOSTS_ENABLED=/etc/httpd/conf.d

# For Ubuntu/Debian, use:
# FREEPANEL_VHOSTS_PATH=/etc/apache2/sites-available
# FREEPANEL_VHOSTS_ENABLED=/etc/apache2/sites-enabled

# DNS Zone Files
FREEPANEL_DNS_ZONES_PATH=/var/named

# Mail Storage
FREEPANEL_MAIL_PATH=/var/mail/vhosts

# User Home Directories
FREEPANEL_HOME_BASE=/home

# SSL Certificates
FREEPANEL_SSL_PATH=/etc/ssl/freepanel

# Backup Storage
FREEPANEL_BACKUPS_PATH=/var/backups/freepanel

#==============================================================================
# LET'S ENCRYPT CONFIGURATION
#==============================================================================

# Email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=

# Use staging server for testing (true/false)
LETSENCRYPT_STAGING=false

#==============================================================================
# SANCTUM (API Authentication)
#==============================================================================

# Comma-separated list of domains that can make stateful requests
SANCTUM_STATEFUL_DOMAINS=localhost:8080,127.0.0.1:8080

#==============================================================================
# MAIL CONFIGURATION (for sending system emails)
#==============================================================================

MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=25
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

#==============================================================================
# OPTIONAL: EXTERNAL SERVICES
#==============================================================================

# AWS S3 for backup storage (optional)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=

# Pusher for real-time notifications (optional)
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_APP_CLUSTER=mt1

#==============================================================================
# OAUTH AUTHENTICATION (Optional)
#==============================================================================

# Supported OAuth providers (comma-separated)
# Available: google,github,microsoft,oidc
# OAUTH_PROVIDERS=google,github

# Google OAuth
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:1455/auth/callback

# GitHub OAuth
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_REDIRECT_URI=http://localhost:1455/auth/callback

# Microsoft OAuth
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
# MICROSOFT_REDIRECT_URI=http://localhost:1455/auth/callback

# Generic OpenID Connect (OIDC)
# OIDC_CLIENT_ID=
# OIDC_CLIENT_SECRET=
# OIDC_REDIRECT_URI=http://localhost:1455/auth/callback
# OIDC_AUTHORIZE_URL=
# OIDC_TOKEN_URL=
# OIDC_USERINFO_URL=
